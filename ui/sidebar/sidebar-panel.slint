import { Palette } from "std-widgets.slint";  

export component SidebarPanelHeader inherits Rectangle {
  in property <string> label;

  background: #1F1F1F;

  HorizontalLayout {
    alignment: start;
    padding-top: 8px;
    padding-bottom: 8px;
    padding-left: 10px;
    padding-right: 10px;

    Text {
      text: label;
    }
  }
}

export component SidebarPanelContent inherits VerticalLayout {
  padding: 10px;
  alignment: start;
}
